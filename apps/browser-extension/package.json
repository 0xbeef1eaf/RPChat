{
  "name": "@rp/browser-extension",
  "version": "0.2.0",
  "private": true,
  "description": "RPChat browser bridge: a Manifest V3 extension that lets characters drive the user's Chromium-based browser through the app's loopback bridge",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@types/chrome": "^0.3.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.9.3",
    "vitest": "^5.0.1"
  }
}
